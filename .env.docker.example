# Troque a senha antes de usar fora do ambiente local.
POSTGRES_DB=totem_ingresso
POSTGRES_USER=totem
POSTGRES_PASSWORD=troque-esta-senha
# Opcional localmente; em produção, informe uma chave Fernet independente da senha do banco.
CPF_ENCRYPTION_KEY=
POSTGRES_PORT=5432
APP_PORT=8080
LOG_LEVEL=INFO
